import 'package:flutter/material.dart';
import 'package:shared_ui/colors/index.dart';

BoxDecoration equipmentsItemNewBookingsBoxDecoration = BoxDecoration(
  borderRadius: const BorderRadius.all(
    Radius.circular(5),
  ),
  border: Border.all(
    width: 1,
    color: lightGreyColor,
    style: BorderStyle.solid,
  ),
);

BoxDecoration meetingRoomItemNewBookingsBoxDecoration = BoxDecoration(
  borderRadius: const BorderRadius.all(
    Radius.circular(5),
  ),
  border: Border.all(
    width: 1,
    color: lightGreyColor,
    style: BorderStyle.solid,
  ),
);
