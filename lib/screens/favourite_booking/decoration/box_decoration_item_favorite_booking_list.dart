import 'package:flutter/material.dart';
import 'package:shared_ui/shared_ui.dart';

// Favorite Screen
BoxDecoration equipmentsItemFavouriteBookingsBoxDecoration = BoxDecoration(
  borderRadius: const BorderRadius.all(
    Radius.circular(5),
  ),
  border: Border.all(
    width: 1,
    color: lightGreyColor,
    style: BorderStyle.solid,
  ),
);

BoxDecoration meetingRoomItemFavouriteBookingsBoxDecoration = BoxDecoration(
  borderRadius: const BorderRadius.all(
    Radius.circular(5),
  ),
  border: Border.all(
    width: 1,
    color: lightGreyColor,
    style: BorderStyle.solid,
  ),
);
